# Normalize text files to LF
* text=auto eol=lf

# Always treat common binary formats as binary (no line-ending normalization, no corruption)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg binary
*.pdf binary